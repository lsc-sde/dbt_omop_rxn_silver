version: 2
sources:
- name: omop
  catalog: hive_metastore
  schema: omop_source
  tables:
    - name: cohort
      columns:
        - name: cohort_definition_id
        - name: subject_id
        - name: cohort_start_date
        - name: cohort_end_date
